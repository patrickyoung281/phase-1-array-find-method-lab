




function superbowlWin (array) {
    let answer = array.find(record => record.result === "W")
    if (answer) {
        return answer.year;
    }
    else return undefined;
}




















/*


function superbowlWin (array) {
    let answer = array.find(record => record.result === "W")
    if (answer) {
        return answer.year;
    }
    else return undefined;
};



*/




















/*



function superbowlWin (array) {
    const win = array.find(gameObj => gameObj.result === "W")
    if (win) {
        return win.year;
    }
    else return undefined
    };




*/

















